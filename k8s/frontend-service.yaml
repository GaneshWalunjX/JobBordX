kind: Service
apiVersion: v1
metadata:
  name: jobboardx-frontend-service
  namespace: jobboardx-ns
spec:
  selector:
    app: jobboardx-frontend
    version: green  
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
